import { DoadorDetalhePipe } from './doador-detalhe.pipe';

describe('DoadorDetalhePipe', () => {
  it('create an instance', () => {
    const pipe = new DoadorDetalhePipe();
    expect(pipe).toBeTruthy();
  });
});
